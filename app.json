{
  "addons": [
    "heroku-postgresql"
  ],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ],
  "description": "",
  "env": {
    "NODE_ENV": {
      "required": true
    }
  },
  "formation": {
    "web": {
      "quantity": 1
    }
  },
  "name": "foosball-rating",
  "scripts": {
    "postdeploy": "cd backend && NODE_TLS_REJECT_UNAUTHORIZED=0 PGSSLMODE=require npm run create-test-db"
  },
  "stack": "container"
}
