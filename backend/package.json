{
  "name": "foosball-backend",
  "version": "0.2.0",
  "description": "The backend part of a Foosball Rating app.",
  "main": "app.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ts-node -r dotenv/config app.ts",
    "migrate": "ts-node node_modules/.bin/node-pg-migrate -j ts -m db_migrations up",
    "create-test-db": "ts-node node_modules/.bin/node-pg-migrate -j ts -m db_test -t pgmigrationstest up"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^7.0.0",
    "express": "^4.16.4",
    "nedb": "^1.8.0",
    "pg": "^7.8.1",
    "slackbots": "^1.2.0"
  },
  "devDependencies": {
    "@types/node": "^13.9.0",
    "node-pg-migrate": "^4.2.2",
    "ts-node": "^8.6.2",
    "typescript": "^3.8.3"
  }
}
